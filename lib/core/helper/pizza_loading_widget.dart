import 'package:flutter/material.dart';

class PizzaLoadingWidget extends StatelessWidget {
  const PizzaLoadingWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const CircularProgressIndicator();
  }
}
